import { useState } from 'react';
import './App.css';
import './Login.css';

const App = () => {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <div className='main-layout'>
      /* Empty page */
    </div>
  )
}